Future:

	* Write portals(LOS).txt parser.
	* Write some more developer documentation.
	* Use memory pools to avoid memory fragmentation and malloc()
	  overhead.
	* Configurable socket path/port.
	* Use shared memory as transport on Windows (maybe Linux too?)

Blue sky future:

	* Plugin support.
	* On-the-fly data file reloading.
