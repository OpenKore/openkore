#include <dirent.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//#include <unistd.h>

int main(int argc, char * const argv[])
{
    /*int c, p = 0;
    extern char *optarg;

    while ((c = getopt(argc, argv, "p:")) != -1) {
        switch(c) {
            case 'p':
                if (optarg == "aegis") p = 1;
                else p = 0;
        }
    }*/
    
    //AegisPortals();
    AthenaPortals();
    
    return 0;
}
